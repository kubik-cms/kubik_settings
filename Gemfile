source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in kubik_settings.gemspec.
gemspec

group :development, :test do
  gem "sqlite3"
  gem "sassc-rails"
end

# To use a debugger
# gem 'byebug', group: [:development, :test]
