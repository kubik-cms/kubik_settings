# frozen_string_literal: true

class CreateKubikSettings < ActiveRecord::Migration<%= migration_version %>
  def change
    create_table :<%= table_name %> do |t|
      t.json :settings_hash, default: {}
      t.integer :singleton_guard, default: 1
      t.timestamps
    end
    add_index :<%= table_name %>, :singleton_guard, unique: true
  end
end
